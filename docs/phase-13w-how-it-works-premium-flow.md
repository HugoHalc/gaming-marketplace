# Phase 13W — How it Works Premium Flow

Redesigns only the Home `How it Works` section.

## What changed
- Keeps the same 3 current steps and same copy.
- Preserves the section heading, title and description on the left.
- Replaces the generic SaaS-style cards with a more connected, premium step sequence.
- Adds large `01 / 02 / 03` step numeration.
- Adds subtle connector lines to reinforce progression.
- Uses restrained dark surfaces with `#FFFFFF14` borders.
- Adds small consistent icons with lower hierarchy than the numbers.
- Keeps hover subtle: tiny lift, soft green border, no heavy glow.
- Uses the approved BoostingPedia palette.

## Intent
The section should visually read as:
game → configure → track

## Unchanged
- page structure
- section order
- copy
- navigation
- functionality
- all other Home sections
