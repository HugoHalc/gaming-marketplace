# Phase 13Q — Cinematic Integrated Hero

Implements the newly approved BoostingPedia hero artwork as an environmental hero layer.

## Changes
- Replaces the hero artwork asset with the new approved image.
- Removes the old contained right-column artwork treatment.
- Renders the artwork as a large absolute layer directly in the hero section.
- Extends the artwork beyond the right side so its physical edge is never visible.
- Uses one long horizontal mask for a clean transition into `#050807`.
- Keeps the emblem sharp and visually dominant.
- Preserves the existing left-side content and layout.
- Uses responsive sizing/positioning so the emblem remains visible on tablet/mobile.

## Unchanged
- hero copy
- label
- CTA buttons
- trust bullets
- hero section structure
- header/navigation
- all other Home sections
- game cards
- Rocket League logic/functionality
