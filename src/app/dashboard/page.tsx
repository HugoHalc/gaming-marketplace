import Link from "next/link"; import { requireUser } from "@/features/auth/server/auth"; import { SiteHeader } from "@/components/marketing/site-header"; import { Container } from "@/components/layout/container";
export const metadata={title:"Dashboard"}; export const dynamic="force-dynamic";
export default async function DashboardPage(){const identity=await requireUser();return <><SiteHeader/><main className="py-12 sm:py-16"><Container><p className="text-sm font-semibold text-violet-300">ACCOUNT</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">Welcome{identity.profile?.full_name?`, ${identity.profile.full_name.split(" ")[0]}`:""}</h1><p className="mt-3 max-w-2xl text-[var(--muted-foreground)]">Manage your profile and keep track of your services from one secure account.</p><div className="mt-8 grid gap-4 md:grid-cols-3"><Link href="/dashboard/profile" className="rounded-2xl border border-white/10 bg-[var(--surface)] p-5 hover:border-white/20"><p className="font-semibold">Profile</p><p className="mt-1 text-sm text-[var(--muted-foreground)]">Update your gaming identity and contact details.</p></Link><Link href="/dashboard/orders" className="rounded-2xl border border-white/10 bg-[var(--surface)] p-5 hover:border-white/20"><p className="font-semibold">Orders</p><p className="mt-1 text-sm text-[var(--muted-foreground)]">Review active services and your complete order history.</p></Link>{identity.profile?.role==="admin"&&<Link href="/admin" className="rounded-2xl border border-violet-400/20 bg-violet-400/10 p-5"><p className="font-semibold">Admin</p><p className="mt-1 text-sm text-violet-200/70">Open the protected administration area.</p></Link>}</div><form action="/auth/signout" method="post" className="mt-8"><button className="rounded-xl border border-white/10 px-4 py-2 text-sm font-semibold hover:bg-white/5">Sign out</button></form></Container></main></>}
