import Link from "next/link"; import { requireAdmin } from "@/features/auth/server/auth"; import { SiteHeader } from "@/components/marketing/site-header"; import { Container } from "@/components/layout/container";
export const metadata={title:"Admin"};export const dynamic="force-dynamic";export default async function AdminPage(){await requireAdmin();return <><SiteHeader/><main className="py-14"><Container><p className="text-sm font-semibold text-violet-300">ADMINISTRATION</p><h1 className="mt-2 text-4xl font-semibold">Admin access verified</h1><p className="mt-3 max-w-2xl text-[var(--muted-foreground)]">This area is protected by authenticated identity plus the server-read profile role. Your administrative tools will live behind this protected access layer.</p><Link href="/dashboard" className="mt-7 inline-flex rounded-xl border border-white/10 px-4 py-2 text-sm font-semibold">Back to dashboard</Link></Container></main></>}
