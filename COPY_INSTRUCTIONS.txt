PHASE 16P12 — UNIFIED DASHBOARD ORDERS ROLE MODE

Replace/add only the files included in this ZIP, preserving the same folder structure.

What this restores:
- /dashboard/orders remains the single orders route.
- Customer mode shows only the customer's own orders and never shows Accept.
- Booster mode uses /dashboard/orders?mode=booster and shows available/active/completed booster orders.
- Available booster cards render a real Accept button.
- After claiming, the order opens at /dashboard/orders/[id]?mode=booster.
- /dashboard/orders/[id] now renders the correct customer or booster workspace according to authorized mode.
- Pure booster-role accounts automatically use booster mode on /dashboard/orders.
- Dual-access accounts can switch between Customer Orders and Booster Orders without reviving /booster/orders.

No Supabase migration is required.

Recommended commit:
fix: restore unified dashboard orders role mode and booster claims
