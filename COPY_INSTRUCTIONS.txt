BOOSTINGPEDIA — PHASE 16M.1
SECURE ACCOUNT ACCESS HOTFIX

PART A — REPLACE THE VERCEL SECRET

In Windows PowerShell generate a fresh 32-byte key LOCALLY:

$bytes = New-Object byte[] 32
[System.Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($bytes)
[Convert]::ToBase64String($bytes)

Copy ONLY the resulting Base64 string.

In Vercel:
Project -> Settings -> Environment Variables

Replace:
BOOSTINGPEDIA_CREDENTIALS_KEY

Use the generated value WITHOUT quotes and WITHOUT spaces.

Apply it to Production (and Preview/Development too if those environments use Secure Account Access).

Do not paste the secret into ChatGPT, GitHub, source code, screenshots, commits, or documentation.

PART B — APPLY CODE HOTFIX

Replace/add the ZIP files.

No Supabase migration.

Commit:
Phase 16M.1 - Fix Secure Account Access Encryption

Push and redeploy AFTER updating the Vercel variable.

TEST

As customer:
1. Open an assigned order.
2. Secure Account Access / Account Details.
3. Save account email + password.
4. Refresh.
5. Confirm state shows credentials saved.

As assigned booster:
6. Open the same order.
7. Reveal/access credentials.
8. Confirm the exact values decrypt correctly.

Expected:
- customer can save
- booster can reveal
- client never sees BOOSTINGPEDIA_CREDENTIALS_KEY or crypto implementation errors
