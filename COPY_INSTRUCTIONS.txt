BOOSTINGPEDIA — PHASE 16D
LIVE ORDER CHAT + SECURE ACCOUNT ACCESS

IMPORTANT:
This phase includes a Supabase migration and requires a new server secret.

APPLY AFTER:
Phase 16A
Phase 16B
Phase 16B2
Phase 16C

1. COPY ZIP INTO PROJECT ROOT.

2. SET ENVIRONMENT SECRET BEFORE TESTING CREDENTIALS:

BOOSTINGPEDIA_CREDENTIALS_KEY=<base64 32-byte random key>

Generate:
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

Add to:
- .env.local
- Vercel project environment variables

DO NOT COMMIT THE KEY.

3. APPLY SUPABASE MIGRATION:

supabase/migrations/phase_16d_live_chat_secure_access.sql

4. DEPLOY APPLICATION.

FUNCTIONAL TESTS:

CHAT:
- customer can send/read messages on own order
- unrelated customer cannot access order chat API
- assigned booster can access assigned order chat
- admin can access order chat
- messages persist after refresh
- chat refreshes approximately every 3 seconds
- Discord / WhatsApp / Telegram / PayPal terms create moderation flags

CREDENTIALS:
- customer can save account email + password
- database row contains ciphertext/iv/auth_tag, not plaintext
- credentials stay hidden by default
- explicit Reveal returns credentials for authorized participant
- unrelated customer cannot reveal credentials
- assigned booster can reveal credentials
- admin can reveal credentials

BOOSTER:
- profile role supports booster
- admin assignment endpoint:
  POST /api/admin/orders/[id]/assign-booster
  JSON: {"boosterId":"<profile uuid>"}
- only profiles with role=booster can be assigned

PAYMENTS:
- Stripe checkout still works unchanged

NOT INCLUDED:
- booster dashboard
- admin assignment UI
- screenshots
- integrity verification
- identity verification

Commit:
Phase 16D - Live Chat Secure Account Access
