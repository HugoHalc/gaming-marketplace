BOOSTINGPEDIA — REALTIME CHAT FIX

Replace only this file in your current GitHub project:

src/components/dashboard/order-live-chat.tsx

What this fixes:
- Authenticates Supabase Realtime with the active user session before subscribing.
- Receives new order chat INSERT events without manually refreshing the page.
- Refreshes messages immediately after the realtime subscription becomes active.
- Refreshes when the browser tab becomes visible again.
- Uses a 5-second fallback only if Realtime is unavailable.
- Preserves the participant name, timezone, online/last-seen UI from the previous patch.

No Supabase migration is required for this fix.
