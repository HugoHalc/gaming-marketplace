BOOSTINGPEDIA — PHASE 16P10
Booster Claim Submission Reliability Fix

Replace these files in the repository, preserving the same paths:

1. src/components/booster/claim-order-button.tsx
2. src/app/api/booster/orders/[id]/claim/route.ts

What changed:
- Accept/Accept Order now submits through a native HTML POST form.
- Claim no longer depends on React client hydration or an onClick fetch handler.
- Successful claims redirect directly to the assigned booster order workspace.
- The existing secure claim_order_for_booster RPC remains authoritative and atomic.
- JSON API behavior is preserved for non-browser-form callers.

Supabase:
- No migration required.
- No database schema changes.

Recommended commit:
fix: make booster order claims work without client hydration
