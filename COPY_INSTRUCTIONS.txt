PHASE 17A.3 — Admin Orders Relationship Fix

ROOT CAUSE
/admin failed with:
Could not embed because more than one relationship was found for 'orders' and 'profiles'

FIX
The admin orders query now explicitly uses the existing foreign key:
orders_user_id_fkey

Changed:
profiles(full_name, gamer_tag, phone)

To:
profiles!orders_user_id_fkey(full_name, gamer_tag, phone)

FILES TO REPLACE
1. src/features/admin/server/admin-orders.ts

SUPABASE
No migration required.

NOT CHANGED
- Live Support
- Dashboard
- Order lifecycle
- Checkout
- Pricing
- RLS
- Auth roles

RECOMMENDED COMMIT
fix: disambiguate admin orders customer profile relation
