PHASE 16P9 — Booster Available Order Accept CTA

Copy the included src/ folder over the repository root, preserving paths and replacing the existing files.

Modified files:
- src/app/booster/orders/page.tsx
- src/components/booster/claim-order-button.tsx

Behavior:
- Available orders show a prominent Accept button in the card header next to the payout.
- The existing secure claim endpoint/RPC is reused.
- Active/completed order behavior is unchanged.
- No Supabase migration is required.

Recommended commit:
feat: surface accept action on available booster orders
