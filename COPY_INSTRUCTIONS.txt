BOOSTINGPEDIA — PHASE 16E.2 SERVICE ROLE HOTFIX

1. COPY THIS ZIP INTO THE PROJECT ROOT.

2. OPEN:
supabase/migrations/phase_16e2_service_role_grants.sql

3. SUPABASE → SQL EDITOR → NEW QUERY.

4. COPY ALL SQL AND PRESS RUN.

EXPECTED:
Success

5. NO DATA INSERTS ARE NEEDED.
Do not recreate your booster profile.
Do not rerun Phase 16D or Phase 16E.

6. This migration is database-only.
Once SQL returns Success, refresh production and test:

/dashboard
/dashboard/orders/[YOUR_ORDER_ID]
/booster

Expected:
- customer dashboard still loads;
- order detail loads;
- booster assignment query no longer errors;
- /booster loads the Available Orders marketplace.

Suggested commit:
Phase 16E.2 - Fix Service Role Grants
