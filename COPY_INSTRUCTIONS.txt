PHASE 16P6 — ORDER ITEM VIRTUAL ID FIX

Commit:
fix: handle virtual catalog ids when creating orders

Copy the included files into the ROOT of the current gaming-marketplace repository, preserving the folder structure and replacing the existing file when prompted.

Modified file:
- src/features/orders/server/order-repository.ts

What this fixes:
- Prevents virtual catalog/navigation IDs such as service_rl_tournament_placeholder from being inserted into UUID foreign-key columns.
- Real UUID game/service IDs are still stored normally.
- Virtual IDs are stored as NULL only in the optional foreign-key columns, while the order snapshot keeps game name, service name, category, configuration, pricing breakdown and rule-set version.
- Prevents the customer-facing error: Unable to create order item.

Supabase migration:
- NOT required.

After copying:
1. Commit the replacement file.
2. Push to main.
3. Let Vercel deploy.
4. Test creating a customer order again.
